import { StyleSheet }  from 'react-native';

const StyleSphlash = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#F5DEB3',
    },
    activityIndicator: {
      alignItems: 'center',
      height: 80,
    },
    imageSphlash: {
        width: '90%', resizeMode: 'contain', margin: 30
    },
  });
export default  StyleSphlash;